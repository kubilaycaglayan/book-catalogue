import React from 'react';
import Query from '../containers/Query';

const QueryPage = () => (
  <div>
    <Query />
    Please type in to search through the books or authors...
  </div>
);

export default QueryPage;
